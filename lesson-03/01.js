// Напишите функцию calculateAverage, которая принимает 2 числа и возвращает их среднее арифметическое

function calculateAverage() {
    let sum = 0;
            for ( i = 0; i < arguments[i]; i++ ) sum += arguments[i];
            return sum == 0 ? sum : sum / arguments.length;
}
